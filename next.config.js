/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  // Add env vars here if required on client side or add prefix `NEXT_PUBLIC_`
  env: {},
};

module.exports = nextConfig;
